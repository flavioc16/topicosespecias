git --version = verifica a versão do git.
git init = inicia  um repositorio na pasta atual.
git config --global user.name "Flavio Sousa de Castro" = Cria usuário do git.
git config --global user.email "flavio2008sousa@hotmail.com" = Cria email do usuário.
git status = Verifica tudo que não foi salvo.
git add . = Prepara aqrquivos para serem transferidos.
git commit -m "Mensagem" = mensagem para identificação do commit.
git log = Mostra todos os logs dos commit realizados.
git remote add origin linkdorepositorio = Vincula a past local ao repositório do GIT.
git push -u origin master = Fazer commit pela primeira vez.